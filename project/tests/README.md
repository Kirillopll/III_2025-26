# tests

Тесты запускаются командой:

```bash
python -m pytest tests
```

Проверяется:

- спецификация признаков;
- преобразование payload в таблицу признаков;
- подготовка supervised-таблицы;
- категоризация риска;
- HTML-страница сервиса;
- `/health`;
- `/predict`.
