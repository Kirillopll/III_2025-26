# configs

В этой папке лежат настройки проекта.

- `model.yaml` - путь к данным, параметры обучения, список моделей и путь к артефакту модели.
- `.env.example` - пример переменных окружения для сервиса.

Реальный `.env` с секретами или локальными настройками не нужно коммитить.
